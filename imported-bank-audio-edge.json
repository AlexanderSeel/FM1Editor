{
  "ok": false,
  "browserName": "edge",
  "error": "Timed out waiting for browser condition: [...document.querySelectorAll('article')].some((article) => article.textContent?.includes('ROM1A') && [...article.querySelectorAll('button')].some((button) => button.textContent?.trim() === 'Load bank' && !button.disabled))"
}
