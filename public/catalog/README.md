# Catalog assets

`sysexFinal.zip` in this directory is the exact user-supplied base archive used by the in-app patch browser.

Website-derived mirrors and `sync-manifest.json` are generated by `npm run catalog:sync` and remain excluded from Git.
